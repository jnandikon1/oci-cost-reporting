---
- name: Generate Daily Excel Report from ADW
  command: >
    {{ ansible_python_interpreter }}
    {{ scripts_dir }}/generate_excel_report.py
    --table {{ adw_target_table }}
    --period DAILY
    --output {{ reports_dir }}/oci_daily_report_{{ ts_file }}.xlsx
  register: report_generation
  changed_when: true
